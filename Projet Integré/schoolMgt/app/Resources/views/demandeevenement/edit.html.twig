{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
    <div class="card bg-light mb-3" style="width: 50%;height: 50%">
        <div class="card-header">Evenement edit</div>
        <div class="card-body">
            <h5 class="card-title"></h5>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('demandeevenement_index') }}">Back to the list</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
        </div></div></div>
{% endblock %}
