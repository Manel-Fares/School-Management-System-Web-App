{% extends 'interfaceEnseignant.html.twig' %}
{% block body %}
    <br><br>
    <div class="col-xl6 col-lg-6 col-md-10 col-sm-10 col-10">
        <div class="card">
            <div class="card-header">
                <h2 class="mb-0">Add Quiz Questions</h2>
            </div>
            <div class="card-body">
                <div class="table-responsive" align="center">

                    <table class="table-borderless" width=80%>

                        {{ form_start(form, {'attr': {'novalidate':'novalidate'}}) }}
                        <td>

                            {{ form(form) }}

                        </td>
                    </table>
                </div>
            </div>
        </div>
    </div>
    </div>
{% endblock %}