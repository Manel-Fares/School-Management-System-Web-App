{% extends 'interfaceEtudiant.html.twig' %}
{% block body %}
    <div class="container" align="center">
    <br>
    <h2 align="center"> Subjects List</h2>
    <br>
    <center>
        <table class="table" width="80%">
            <thead style="background-color: #a3a3cb" >
                 <tr>
                    <th>Name</th>
                   <th>Coefficient</th>
                   <th>Responsable</th>
                     <th>Email</th>

                 </tr>
            </thead>
    {% for m in matier %}
        <tr>
            <td><a href="{{ path('Chapitre_affich',{'id':m}) }}">{{ m.nom }}</a></td>
            <td>{{ m.coef }}</td>
            <td>{{ m.responsable.prenomuser }}  {{ m.responsable.nomuser }}</td>
            <td>{{ m.responsable.email }}  </td>

        </tr>
    {% endfor %}
</table>
    </center>
    </div>
{% endblock %}