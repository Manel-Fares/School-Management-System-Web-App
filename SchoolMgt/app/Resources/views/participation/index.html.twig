{% extends 'base.html.twig' %}

{% block body %}
    <h1>Participations list</h1>

    <table>
        <thead>
            <tr>
                <th>Idparticipation</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for participation in participations %}
            <tr>
                <td><a href="{{ path('participation_show', { 'idparticipation': participation.idparticipation }) }}">{{ participation.idparticipation }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('participation_show', { 'idparticipation': participation.idparticipation }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('participation_edit', { 'idparticipation': participation.idparticipation }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('participation_new') }}">Create a new participation</a>
        </li>
    </ul>
{% endblock %}
