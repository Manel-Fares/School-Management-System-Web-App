{% extends 'interfacePersonnel.html.twig' %}
{% block body %}
  {#  <h1>Book creation</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('books_index') }}">Back to the list</a>
        </li>
    </ul>
    #}

    <div class="container-fluid  dashboard-content">
    <!-- ============================================================== -->
    <!-- pageheader -->
    <!-- ============================================================== -->

    <div class="row">
        <div class="card">
    <table class="table table-striped table-responsive-md btn-table">

        <thead>
        <tr>
            <th>#</th>
            <th>Title</th>
            <th>Descr</th>
            <th>Status</th>
            <th>Categ</th>

            <th>Action</th>
        </tr>
        </thead>
        {% for key,book in books  %}
            <tbody>
            <tr>
                <th scope="row">{{ key+1 }}</th>
                <td>{{ book.titrebook }}</td>
                <td>{{ book.descriptionbook }}</td>
                <td>{{ book.etatbook }}</td>
                <td>{{ book.categoriebook }}</td>
                <td><a href="{{ path('books_edit', { 'idbook': book.idbook }) }}">   <button type="button" class="btn btn-success" >UPDATE</button></a></td>

            </tr>
            </tbody>

        {% endfor %}
    </table>

    </div>
    </div>
    </div>

{% endblock %}